version: '3.8'

services:
  web:
    build: .
    container_name: ciencia_dados_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - PORT=5000
      - FLASK_ENV=development
    restart: unless-stopped
